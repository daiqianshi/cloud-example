spring:
  cloud:
    config:
      label: master
      profile: dev
      #uri: http://localhost:8888/
      discovery:
        enabled: true
        service-id: server-config
    bus:
      trace:
        enabled: true
      enabled: true
  #zipkin:
  #  base-url: http://localhost:9411
  #  sender:
  #    type: web
  #sleuth:
  #  sampler:
  #    probability : 1